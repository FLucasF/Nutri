spring:
  datasource:
    url: jdbc:h2:mem:nutriplan-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    clean-disabled: false

nutriplan:
  jwt:
    secret: segredo-de-teste-com-mais-de-32-bytes-aqui-ok
    expiration-minutes: 60
