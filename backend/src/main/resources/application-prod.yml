spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nutriplan}
    username: ${DB_USER:nutriplan}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: false
